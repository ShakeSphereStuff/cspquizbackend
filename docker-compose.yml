version: '3'
services:
        web:
                image: flask_port_v1
                build: .
                ports:
                        - "8085:8085"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
